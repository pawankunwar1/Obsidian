this file contains important components of an application like databases passwords, secret keys etc.

for mysql -> we use -> `"ConnectionStrings": {"DefaultConnection": "Server=localhost;User ID=pk;password=kuwar;"}`